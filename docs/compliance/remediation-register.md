# Remediation Register

> The single, prioritised backlog of control gaps surfaced by this compliance package and the
> [`specs/security/threat-model.md`](../../specs/security/threat-model.md). **REM-001…004 are
> owned by the threat model** (reproduced here for one consolidated view); **REM-005+ are new,
> surfaced by the ISO/SOC 2/SLSA assessment.** When an item lands, flip its row(s) in the
> [control matrix](iso27001-annex-a-control-matrix.md) to ✅ and move it to **Done** below.
>
> **Last updated:** 2026-05-29 · Owners reference `.github/CODEOWNERS` roles (currently placeholders — see REM-009).

## Priority legend

`P0` blocks any production handling of real data/customers · `P1` blocks enterprise engagement ·
`P2` required for certification (SOC 2 / ISO) · `P3` hygiene.

---

## Open items

| ID          | Severity | Gap                                                                                                                                                                                                                                                                | Why it matters (framework)                                                                                                                | Owner                   | Suggested target                        |
| ----------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | --------------------------------------- |
| **REM-009** | **P1**   | CODEOWNERS uses placeholder `@org/*` teams (don't resolve); DPIA is **Draft** (no DPO sign-off)                                                                                                                                                                    | Controls that name an owner who doesn't exist are not auditable; DPIA must be Approved for GDPR/LGPD. ISO 5.2/5.31                        | Tech Lead + DPO         | Before first enterprise engagement      |
| **REM-002** | P1       | `LLMTokenBudgetExceeded90Percent` alert not wired to PagerDuty                                                                                                                                                                                                     | Cost-exhaustion DoS goes unactioned. ISO 8.16 _(threat-model)_                                                                            | SRE Lead                | Wave B                                  |
| **REM-003** | P1       | No **mTLS** between pods (Kafka/internal traffic plaintext)                                                                                                                                                                                                        | Lateral-movement / eavesdropping risk. ISO 5.14/8.20, SOC 2 CC6.7 _(threat-model)_                                                        | DevOps Lead             | Wave C — blocked on ADR-0007 + cluster  |
| **REM-004** | P2       | DAST not run in `ci.yml` (OWASP ZAP runs only in `staging-check.yml`)                                                                                                                                                                                              | Shift DAST left. ISO 8.29 _(threat-model)_                                                                                                | DevSecOps               | Wave C                                  |
| **REM-011** | P2       | **Supply-chain remainder of REM-007:** OIDC for registry/cloud auth (replace long-lived `REGISTRY_USERNAME`/`PASSWORD`); admission-time image-signature verification (Kyverno/cosign policy); replace `Syft`/`Cosign` `curl \| sh` installs with pinned installers | Completes SLSA L2→L3. **Needs real cloud OIDC role + cluster policy — not implementable in the template alone.** ISO 5.21/8.19, SOC 2 CC9 | DevSecOps + DevOps Lead | Wave C — when cloud/cluster provisioned |
| **REM-014** | P2       | No Prometheus alerting rules file committed; `circuit_breaker_state` metric missing from `retry.py`; SLO burn-rate alerts and `ConsumerStale` rule exist only as runbook references                                                                                | Alerts that don't exist as rules never fire — SLO/circuit-breaker state invisible to on-call. ISO 8.16, SOC 2 CC7.2                       | SRE Lead                | Wave B                                  |

| ID          | Severity           | Gap                                                                                                                                                                                                                                                                | Why it matters (framework)                                                                                                                | Owner                   | Suggested target                        |
| ----------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | --------------------------------------- |
| **REM-009** | **P1** _(partial)_ | **CODEOWNERS ✅ resolved** (all `@org/*` replaced with `@valdomirosouza`; CI gate added). **DPIA still Draft** — DPO sign-off not yet obtained. Follow `docs/governance/owner-onboarding.md` Step 5.                                                               | DPIA must reach Approved before enterprise deployment. ISO 5.31, GDPR Art. 35.                                                            | DPO                     | Before first enterprise engagement      |
| **REM-002** | P1                 | `LLMTokenBudgetExceeded90Percent` alert not wired to PagerDuty                                                                                                                                                                                                     | Cost-exhaustion DoS goes unactioned. ISO 8.16 _(threat-model)_                                                                            | SRE Lead                | Next release                            |
| **REM-003** | P1                 | No **mTLS** between pods (Kafka/internal traffic plaintext)                                                                                                                                                                                                        | Lateral-movement / eavesdropping risk. ISO 5.14/8.20, SOC 2 CC6.7 _(threat-model)_                                                        | DevOps Lead             | Per ADR-0007 (service mesh)             |
| **REM-004** | P2                 | DAST not run in `ci.yml` (OWASP ZAP runs only in `staging-check.yml`)                                                                                                                                                                                              | Shift DAST left. ISO 8.29 _(threat-model)_                                                                                                | DevSecOps               | Next release                            |
| **REM-011** | P2                 | **Supply-chain remainder of REM-007:** OIDC for registry/cloud auth (replace long-lived `REGISTRY_USERNAME`/`PASSWORD`); admission-time image-signature verification (Kyverno/cosign policy); replace `Syft`/`Cosign` `curl \| sh` installs with pinned installers | Completes SLSA L2→L3. **Needs real cloud OIDC role + cluster policy — not implementable in the template alone.** ISO 5.21/8.19, SOC 2 CC9 | DevSecOps + DevOps Lead | When cloud/cluster infra is provisioned |

## Done

| ID                      | Severity | Gap                                                                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Date       |
| ----------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **REM-012**             | Critical | `enable_auto_commit=True` committed offsets before processing completed; no retry or DLQ path — risk of silent message loss or infinite poison-message reprocessing | `enable_auto_commit=False`; offset committed only after `_handle()` returns. Configurable retry loop (3 attempts, exponential backoff). Exhausted retries route to `domain.request.dlq` via broker, increment `DLQ_MESSAGES_COUNTER`, set status=failed. `RequestConsumer` now accepts `broker` param; wired in lifespan. DLQ channel registered in AsyncAPI + services.yaml. New runbook `docs/sre/runbooks/dlq-accumulating.md`. Spec updated. 35 unit tests (all passing). ISO 8.16, SOC 2 CC7.2/CC9 | 2026-05-29 |
| **REM-013**             | Critical | No metric updated by the consumer loop; hung `async for` fails silently while pod appears healthy and Kafka lag grows                                               | Added `CONSUMER_HEARTBEAT_TIMESTAMP` Gauge (epoch seconds) to `src/observability/metrics.py`. Updated after every committed message in `run()`. Alert condition documented: `time() - consumer_heartbeat_timestamp_seconds > 300 AND kafka_consumer_lag > 0`. ISO 8.16, SOC 2 CC7.2                                                                                                                                                                                                                     | 2026-05-29 |
| **REM-001**             | P0       | HITL decision endpoint had **no operator authentication**; `approver_id` came from the request body (impersonation / audit forgery)                                 | JWT bearer auth + `hitl-operator` role check on `POST /v1/hitl/requests/{id}/decision`; approver identity now taken from the verified token subject. `src/api/rest/auth.py`; unit tests for 401/403/identity-from-token. ISO 5.15/8.5, SOC 2 CC6.1                                                                                                                                                                                                                                                      | 2026-05-29 |
| **REM-005**             | P1       | `auto-merge.yml` auto-approved + merged all PRs, bypassing four-eyes / change management                                                                            | Auto-merge now **scoped to documentation-only or Dependabot PRs**; any code/infra/workflow change falls back to mandatory human review. ISO 5.3/8.4/8.32, SOC 2 CC8.1                                                                                                                                                                                                                                                                                                                                   | 2026-05-29 |
| **REM-006**             | P2       | No container image CVE scan in CI                                                                                                                                   | **Trivy** scan added to `ci.yml`'s build job — fails on fixable CRITICAL/HIGH CVEs (`ignore-unfixed`). ISO 8.7, SOC 2 CC6.8                                                                                                                                                                                                                                                                                                                                                                             | 2026-05-29 |
| **REM-007** _(partial)_ | P2       | Falsifiable build / over-broad workflow permissions / no SLSA provenance                                                                                            | **SHA-pinned all 17 GitHub Actions** to commit digests (version comments retained); added least-privilege top-level `permissions:` to the 10 workflows lacking them; added signed **SLSA build provenance** (`actions/attest-build-provenance`) to `release.yml`. **Remaining OIDC + admission-verification split to REM-011.** ISO 5.21, SOC 2 CC9, SLSA                                                                                                                                               | 2026-05-29 |
| **REM-008**             | P2       | Documented conventions not CI-enforced (spec reference, CHANGELOG, Conventional Commits)                                                                            | New `.github/workflows/pr-governance.yml` enforces a Conventional-Commit PR title, a CHANGELOG `[Unreleased]` entry (docs-only / `skip-changelog` / Dependabot exempt), and a spec reference for feat/fix/security/privacy/perf PRs (`no-spec` label / Dependabot exempt); harness `spec-compliance` flipped to `blocking: true`. All four gates are now **merge-blocking** — added to `main` branch protection's required status checks with **strict mode** on. ISO 5.36, SOC 2 CC5                   | 2026-05-29 |
| **REM-010**             | P3       | `version.txt` (1.9.1) was out of sync with `pyproject.toml` (1.15.0)                                                                                                | Reconciled `version.txt` → `1.15.0`; added a `version-consistency` CI gate (in `pr-governance.yml`) that fails any PR which changes `version.txt` or `pyproject.toml` without keeping them equal (release-please bot exempt). ISO 5.36                                                                                                                                                                                                                                                                  | 2026-05-29 |
| **REM-003** | P1       | No **mTLS** between pods (Kafka/internal traffic plaintext)                                                                                                                                                                                                        | Lateral-movement / eavesdropping risk. ISO 5.14/8.20, SOC 2 CC6.7 _(threat-model)_                                                        | DevOps Lead             | Wave C — blocked on ADR-0007 + cluster  |
| **REM-004** | P2       | DAST not run in `ci.yml` (OWASP ZAP runs only in `staging-check.yml`)                                                                                                                                                                                              | Shift DAST left. ISO 8.29 _(threat-model)_                                                                                                | DevSecOps               | Wave C                                  |
| **REM-011** | P2       | **Supply-chain remainder of REM-007:** OIDC for registry/cloud auth (replace long-lived `REGISTRY_USERNAME`/`PASSWORD`); admission-time image-signature verification (Kyverno/cosign policy); replace `Syft`/`Cosign` `curl \| sh` installs with pinned installers | Completes SLSA L2→L3. **Needs real cloud OIDC role + cluster policy — not implementable in the template alone.** ISO 5.21/8.19, SOC 2 CC9 | DevSecOps + DevOps Lead | Wave C — when cloud/cluster provisioned |

## Done

| ID                      | Severity | Gap                                                                                                                                                                                                                                            | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Date       |
| ----------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **REM-002**             | P1       | `LLMTokenBudgetExceeded90Percent` alert rule existed but was not wired to PagerDuty — cost-exhaustion DoS went unactioned                                                                                                                      | Alertmanager service added to `docker-compose.yml` (`prom/alertmanager:v0.27.0`); `infrastructure/monitoring/alertmanager/alertmanager.yml` committed with PagerDuty receiver (`severity=critical` route). Alertmanager block uncommented in `prometheus.yml`. `PAGERDUTY_INTEGRATION_KEY` added to `.env.example`. All `severity=critical` alerts (including `LLMTokenBudgetExceeded90Percent`) now route to PagerDuty when the integration key is set. ISO 8.16, SOC 2 CC7.2                                                                                                                                            | 2026-05-29 |
| **REM-014**             | P2       | No Prometheus alerting rules file committed; `circuit_breaker_state` metric missing; SLO burn-rate rules incomplete (event consumer and agent action tiers absent); `ConsumerStale` and `CircuitBreakerOpen` alerts had no corresponding rules | New `infrastructure/monitoring/prometheus/rules/resilience-alerts.yaml`: `CircuitBreakerOpen` (1=OPEN), `CircuitBreakerHalfOpen` (0.5), `ConsumerStale` (heartbeat + lag guard), `ConsumerLagCritical`. Added `CIRCUIT_BREAKER_STATE` Gauge to `src/observability/metrics.py`; `CircuitBreaker(name=...)` emits state on open/close/half-open transitions; `ResilientLLMClientWrapper` defaults to `name="llm"`, `ResilientDBPool` to `name="db"`. New SLO groups in `slo-burn-rate.yaml`: agent action success rate (fast+slow burn) and event consumer DLQ proxy. 5 new unit tests (all passing). ISO 8.16, SOC 2 CC7.2 | 2026-05-29 |
| **REM-001**             | P0       | HITL decision endpoint had **no operator authentication**; `approver_id` came from the request body (impersonation / audit forgery)                                                                                                            | JWT bearer auth + `hitl-operator` role check on `POST /v1/hitl/requests/{id}/decision`; approver identity now taken from the verified token subject. `src/api/rest/auth.py`; unit tests for 401/403/identity-from-token. ISO 5.15/8.5, SOC 2 CC6.1                                                                                                                                                                                                                                                                                                                                                                        | 2026-05-29 |
| **REM-005**             | P1       | `auto-merge.yml` auto-approved + merged all PRs, bypassing four-eyes / change management                                                                                                                                                       | Auto-merge now **scoped to documentation-only or Dependabot PRs**; any code/infra/workflow change falls back to mandatory human review. ISO 5.3/8.4/8.32, SOC 2 CC8.1                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-05-29 |
| **REM-006**             | P2       | No container image CVE scan in CI                                                                                                                                                                                                              | **Trivy** scan added to `ci.yml`'s build job — fails on fixable CRITICAL/HIGH CVEs (`ignore-unfixed`). ISO 8.7, SOC 2 CC6.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 2026-05-29 |
| **REM-007** _(partial)_ | P2       | Falsifiable build / over-broad workflow permissions / no SLSA provenance                                                                                                                                                                       | **SHA-pinned all 17 GitHub Actions** to commit digests (version comments retained); added least-privilege top-level `permissions:` to the 10 workflows lacking them; added signed **SLSA build provenance** (`actions/attest-build-provenance`) to `release.yml`. **Remaining OIDC + admission-verification split to REM-011.** ISO 5.21, SOC 2 CC9, SLSA                                                                                                                                                                                                                                                                 | 2026-05-29 |
| **REM-008**             | P2       | Documented conventions not CI-enforced (spec reference, CHANGELOG, Conventional Commits)                                                                                                                                                       | New `.github/workflows/pr-governance.yml` enforces a Conventional-Commit PR title, a CHANGELOG `[Unreleased]` entry (docs-only / `skip-changelog` / Dependabot exempt), and a spec reference for feat/fix/security/privacy/perf PRs (`no-spec` label / Dependabot exempt); harness `spec-compliance` flipped to `blocking: true`. All four gates are now **merge-blocking** — added to `main` branch protection's required status checks with **strict mode** on. ISO 5.36, SOC 2 CC5                                                                                                                                     | 2026-05-29 |
| **REM-010**             | P3       | `version.txt` (1.9.1) was out of sync with `pyproject.toml` (1.15.0)                                                                                                                                                                           | Reconciled `version.txt` → `1.15.0`; added a `version-consistency` CI gate (in `pr-governance.yml`) that fails any PR which changes `version.txt` or `pyproject.toml` without keeping them equal (release-please bot exempt). ISO 5.36                                                                                                                                                                                                                                                                                                                                                                                    | 2026-05-29 |

| ID          | Severity       | Gap                                                                                                                                                                                                                                                         | Why it matters (framework)                                                                                         | Owner                   | Suggested target                     |
| ----------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------- | ------------------------------------ |
| **REM-009** | **P1**         | CODEOWNERS uses placeholder `@org/*` teams (don't resolve); DPIA is **Draft** (no DPO sign-off)                                                                                                                                                             | Controls that name an owner who doesn't exist are not auditable; DPIA must be Approved for GDPR/LGPD. ISO 5.2/5.31 | Tech Lead + DPO         | Before first enterprise engagement   |
| **REM-003** | P1 _(partial)_ | **Phase 1 done** (NetworkPolicy manifests + ADR-0007 Accepted: Istio). **Phase 2 blocked**: Istio sidecar STRICT mTLS requires a running cluster — activate with `infrastructure/k8s/network-policies/istio-peer-auth.yaml` once Kubernetes is provisioned. | Lateral-movement / eavesdropping risk. ISO 5.14/8.20, SOC 2 CC6.7                                                  | DevOps Lead             | Phase 2: when cluster is provisioned |
| **REM-011** | P2             | **Supply-chain remainder of REM-007:** OIDC for registry/cloud auth; admission-time image-signature verification (Kyverno/cosign); replace `curl \| sh` installs with pinned binaries                                                                       | SLSA L2→L3. **Needs real cloud OIDC role + cluster policy.** ISO 5.21/8.19, SOC 2 CC9                              | DevSecOps + DevOps Lead | When cloud/cluster provisioned       |

## Done

| ID                      | Severity | Gap                                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Date       |
| ----------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **REM-004**             | P2       | DAST not run in `ci.yml` — OWASP ZAP ran only in `staging-check.yml`; high-severity vulnerabilities only caught post-deploy         | New `dast` job in `.github/workflows/ci.yml`: starts the FastAPI app in in-memory mode (no DB/Redis/Kafka required via fallback pattern), then runs `zap-baseline.py` with `--network host` against `http://localhost:8000`. Fails on FAIL-level ZAP findings (high severity/confidence); WARN findings visible in artifact but non-blocking. ZAP report uploaded as artifact (30-day retention). Job runs after `lint`, parallel with test jobs; `build` job now also `needs: dast`. Trivy `--scanners config` step also added to `build` job for IaC/Helm misconfiguration scanning (catches missing `securityContext`, privileged containers). ISO 8.29, SOC 2 CC7.1 | 2026-05-29 |
| **REM-001**             | P0       | HITL decision endpoint had **no operator authentication**; `approver_id` came from the request body (impersonation / audit forgery) | JWT bearer auth + `hitl-operator` role check on `POST /v1/hitl/requests/{id}/decision`; approver identity now taken from the verified token subject. `src/api/rest/auth.py`; unit tests for 401/403/identity-from-token. ISO 5.15/8.5, SOC 2 CC6.1                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-05-29 |
| **REM-005**             | P1       | `auto-merge.yml` auto-approved + merged all PRs, bypassing four-eyes / change management                                            | Auto-merge now **scoped to documentation-only or Dependabot PRs**; any code/infra/workflow change falls back to mandatory human review. ISO 5.3/8.4/8.32, SOC 2 CC8.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 2026-05-29 |
| **REM-006**             | P2       | No container image CVE scan in CI                                                                                                   | **Trivy** scan added to `ci.yml`'s build job — fails on fixable CRITICAL/HIGH CVEs (`ignore-unfixed`). ISO 8.7, SOC 2 CC6.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 2026-05-29 |
| **REM-007** _(partial)_ | P2       | Falsifiable build / over-broad workflow permissions / no SLSA provenance                                                            | **SHA-pinned all 17 GitHub Actions** to commit digests (version comments retained); added least-privilege top-level `permissions:` to the 10 workflows lacking them; added signed **SLSA build provenance** (`actions/attest-build-provenance`) to `release.yml`. **Remaining OIDC + admission-verification split to REM-011.** ISO 5.21, SOC 2 CC9, SLSA                                                                                                                                                                                                                                                                                                               | 2026-05-29 |
| **REM-008**             | P2       | Documented conventions not CI-enforced (spec reference, CHANGELOG, Conventional Commits)                                            | New `.github/workflows/pr-governance.yml` enforces a Conventional-Commit PR title, a CHANGELOG `[Unreleased]` entry (docs-only / `skip-changelog` / Dependabot exempt), and a spec reference for feat/fix/security/privacy/perf PRs (`no-spec` label / Dependabot exempt); harness `spec-compliance` flipped to `blocking: true`. All four gates are now **merge-blocking** — added to `main` branch protection's required status checks with **strict mode** on. ISO 5.36, SOC 2 CC5                                                                                                                                                                                   | 2026-05-29 |
| **REM-010**             | P3       | `version.txt` (1.9.1) was out of sync with `pyproject.toml` (1.15.0)                                                                | Reconciled `version.txt` → `1.15.0`; added a `version-consistency` CI gate (in `pr-governance.yml`) that fails any PR which changes `version.txt` or `pyproject.toml` without keeping them equal (release-please bot exempt). ISO 5.36                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-05-29 |

| ID                      | Severity | Gap                                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Date       |
| ----------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **Wave E items**        | Low      | Operational resilience depth — eight backlog items from the hardening plan                                                          | ① HTTP security headers (`SecurityHeadersMiddleware`: HSTS prod-only, X-Content-Type-Options, X-Frame-Options, CSP `default-src 'none'`, Referrer-Policy, Permissions-Policy). ② Per-JWT-subject rate limiting (`_limiter.py` key func: JWT sub → per-user bucket, IP fallback). ③ DB pool saturation metrics (`DB_POOL_CONNECTIONS_ACQUIRED/AVAILABLE` Gauges emitted by `ResilientDBPool._call()`). ④ Container image digest pinned: `python:3.13-slim` → `python:3.13.7-slim` in Dockerfile. ⑤ CodeQL analysis workflow (`.github/workflows/codeql.yml`, Python, `security-extended` queries, weekly schedule). ⑥ Redis Sentinel HA config (`redis_sentinel_enabled/master_name/hosts` settings + runbook `docs/sre/runbooks/redis-ha.md`). ⑦ DB key-rotation runbook (`docs/sre/runbooks/db-key-rotation.md` — zero-downtime dual-key procedure). ⑧ `.env.example` Sentinel vars. 9 tests (455 passing, 1 skipped-without-PyJWT). ISO 8.7/8.16/8.20, SOC 2 CC6.8/CC7.2 | 2026-05-29 |
| **REM-001**             | P0       | HITL decision endpoint had **no operator authentication**; `approver_id` came from the request body (impersonation / audit forgery) | JWT bearer auth + `hitl-operator` role check on `POST /v1/hitl/requests/{id}/decision`; approver identity now taken from the verified token subject. `src/api/rest/auth.py`; unit tests for 401/403/identity-from-token. ISO 5.15/8.5, SOC 2 CC6.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-05-29 |
| **REM-005**             | P1       | `auto-merge.yml` auto-approved + merged all PRs, bypassing four-eyes / change management                                            | Auto-merge now **scoped to documentation-only or Dependabot PRs**; any code/infra/workflow change falls back to mandatory human review. ISO 5.3/8.4/8.32, SOC 2 CC8.1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-05-29 |
| **REM-006**             | P2       | No container image CVE scan in CI                                                                                                   | **Trivy** scan added to `ci.yml`'s build job — fails on fixable CRITICAL/HIGH CVEs (`ignore-unfixed`). ISO 8.7, SOC 2 CC6.8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 2026-05-29 |
| **REM-007** _(partial)_ | P2       | Falsifiable build / over-broad workflow permissions / no SLSA provenance                                                            | **SHA-pinned all 17 GitHub Actions** to commit digests (version comments retained); added least-privilege top-level `permissions:` to the 10 workflows lacking them; added signed **SLSA build provenance** (`actions/attest-build-provenance`) to `release.yml`. **Remaining OIDC + admission-verification split to REM-011.** ISO 5.21, SOC 2 CC9, SLSA                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 2026-05-29 |
| **REM-008**             | P2       | Documented conventions not CI-enforced (spec reference, CHANGELOG, Conventional Commits)                                            | New `.github/workflows/pr-governance.yml` enforces a Conventional-Commit PR title, a CHANGELOG `[Unreleased]` entry (docs-only / `skip-changelog` / Dependabot exempt), and a spec reference for feat/fix/security/privacy/perf PRs (`no-spec` label / Dependabot exempt); harness `spec-compliance` flipped to `blocking: true`. All four gates are now **merge-blocking** — added to `main` branch protection's required status checks with **strict mode** on. ISO 5.36, SOC 2 CC5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-05-29 |
| **REM-010**             | P3       | `version.txt` (1.9.1) was out of sync with `pyproject.toml` (1.15.0)                                                                | Reconciled `version.txt` → `1.15.0`; added a `version-consistency` CI gate (in `pr-governance.yml`) that fails any PR which changes `version.txt` or `pyproject.toml` without keeping them equal (release-please bot exempt). ISO 5.36                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 2026-05-29 |

---

## Notes on sequencing

- **REM-001 and REM-005 (Done) were the two that change a "yes/no" answer on an enterprise
  security questionnaire** — closed first by design.
- **REM-006 + REM-007 (Done)** moved the build/release pipeline from SLSA L1-partial toward a
  clean **L2**. The **L3 / OIDC** remainder is **REM-011** (needs real cloud/cluster infra).
- **REM-008 (Done)** closes the "documented ≠ enforced" gap auditors probe — the conventions in
  CLAUDE.md are now CI gates, and the four `pr-governance` checks are required status checks on
  `main` (strict mode on), so they block merges.
- **REM-009 is a force-multiplier:** until the placeholder roles are real people and the DPIA is
  Approved, ~22 "Partial" controls cannot move to "Implemented" no matter what code lands.
- Remaining open: REM-002, REM-003, REM-004 (threat-model items), REM-009, REM-011.
