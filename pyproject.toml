[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project]
name = "template-service"
version = "1.3.0"
description = "Enterprise AI-powered system — production-ready monorepo template"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "httpx>=0.27.0",
    "aiokafka>=0.11.0",
    "redis[asyncio]>=5.0.0",
    "asyncpg>=0.29.0",
    "alembic>=1.13.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "opentelemetry-sdk>=1.25.0",
    "opentelemetry-exporter-otlp-proto-grpc>=1.25.0",
    "opentelemetry-instrumentation-fastapi>=0.46b0",
    "prometheus-client>=0.20.0",
    "structlog>=24.1.0",
    "anthropic>=0.28.0",
    "tenacity>=8.3.0",
    "slowapi>=0.1.9",
    "openfeature-sdk>=0.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
    "bandit[toml]>=1.7.9",
    "pip-audit>=2.7.0",
    "detect-secrets>=1.5.0",
    "gibberish-detector>=0.1.0",
    "pre-commit>=3.7.0",
    "mkdocs-material>=9.5.0",
    "types-redis>=4.6.0",
    "fakeredis>=2.0.0",
]

# ── Ruff ──────────────────────────────────────────────────────────────────────
[tool.ruff]
target-version = "py313"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "S",   # flake8-bandit (security)
    "T20", # flake8-print (no print() in src/)
    "RUF", # ruff-specific rules
]
ignore = [
    "S101", # assert allowed in tests
    "S105", # hardcoded passwords — detect-secrets handles this
    "B008", # do not perform function calls in default arguments (FastAPI Depends)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "T20"]
"alembic/**" = ["S608"]  # Dynamic SQL is standard in Alembic migrations

[tool.ruff.lint.isort]
known-first-party = ["src"]

# ── Mypy ──────────────────────────────────────────────────────────────────────
[tool.mypy]
python_version = "3.13"
strict = true
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = [
    "aiokafka.*",
    "asyncpg.*",
    "confluent_kafka.*",
    "opentelemetry.*",
    "prometheus_client.*",
    "alembic.*",
    "slowapi.*",
    "fakeredis.*",
    "openfeature.*",
]
ignore_missing_imports = true

# ── Pytest ────────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
]
markers = [
    "unit: fast, no I/O",
    "integration: requires running services (postgres, redis, kafka)",
    "security: defensive validation tests",
    "chaos: game-day / fault injection tests",
]

# ── Bandit ────────────────────────────────────────────────────────────────────
[tool.bandit]
exclude_dirs = ["tests", ".venv"]
skips = ["B101"]  # assert_used — allowed in tests
